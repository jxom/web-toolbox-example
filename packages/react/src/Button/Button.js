import React from 'react';
import PropTypes from 'prop-types';

export const Button = () => (
  <button>
    test
  </button>
);

Button.propTypes = {

};

Button.defaultProps = {

};

export default Button;
